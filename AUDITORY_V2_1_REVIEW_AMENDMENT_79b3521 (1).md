# Auditory 下一轮方案：基于 79b3521 的 v2.1 审阅修订

- 审阅日期：2026-09-17。
- 审阅基准：`W-Yinghao/auditory`，commit `79b3521ab9b3fdd533f51192b4ba2e7937509c7a`。
- 科研汇总版本：`reports/auditory_next_v2/final_002/`。`final_001` 不是最终验收依据。
- 被修订的原方案：`AUDITORY_NEXT_ROUND_SERVER_PLAN_v2.md`，Git blob `3791337f7136493305078fc1a1c3baad8c33414c`；与本次对话提供的文件逐字节一致。
- 本文件性质：**新的审阅与实施建议，尚未运行，不覆盖原方案、原结果或失败回执。** 本次没有修改 GitHub、连接服务器或提交 Slurm 作业。
- 范围：保留 G0、A2、C2、N1、N2、N3 名称。B/D 旧研究不重启，E1 不启动。关注 EEG 解码与信息论，不改做 ERP 峰值、量表搜索或新的大型模型项目。

## 0. 核心决定

**需要修改 v2，但不是把所有路线重新跑一遍。** 当前优先级是修复“某个估计器没有成功”和“目标信息没有证据”之间的推断环节。

下一阶段应是：

1. 用既有产物完成 A2 的有限线索复核、G0 的解释性收尾和 N2 的元数据支持检查。
2. 在合成数据上检验一个保留已有基线能力的 N1/N3 增量读出器，先验证真实输入规模下的识别能力。
3. 只有相关能力检验通过、数据支持存在时，才执行一次冻结后的对应真实比较。
4. 不把新算法的结果回填为 v2 成功，不把有限预算学习器改名为“原求解器终于收敛”。

旧 v2 的“不新训正式编码器”是该轮资源安排，不是永久的科学假设；但本修订的第一阶段仍然是 **0 个新正式编码器**。不得因 SimCLR 是旧主表征而要求所有科学现象必须只在 SimCLR 上成立。

## 1. 本轮证据：事实、含义与不能推出的结论

下表数值来自固定公开聚合；CI 是固定预测/统计的候选簇 bootstrap，不是重拟合整个学习过程的区间。[S1–S4]

| 工作包 | 核实的结果 | 可以采取的研究决定 | 不可推出 |
|---|---|---|---|
| G0 | 同儿童后块 R_SIM bAcc 约 0.51572；L0 约 0.53835；共同试次的因果/离线 L0 bAcc 约 0.52791/0.52491 | “只是跨被试失配”缺少积极支持；不继续靠加大网络作为默认解法 | 原始 EEG 没有刺激信息；同儿童固定表征读出是真实信息上限 |
| A2 主 R_SIM | 49 组，未校正 T=0.015389，CI [-0.030596, 0.059324] | 不推进旧 R_SIM 刺激特异性主张；不以校正残差替换 | 已证明没有达到 0.05 的效应；CI 实际仍覆盖该量级 |
| A2 次要 R_SUP | 同 49 组，未校正 T=0.056519，CI [0.015913,0.098295]；post−pre=0.063120，CI [0.009464,0.117336] | 保存为有限复核线索，优先补齐相关质量/背景对照 | R_SUP 显著优于 R_SIM 或随机编码器；旧主结果变为阳性 |
| A2 R_RAND | 未校正 T=0.042133，CI [-0.004072,0.085321] | 学习优势必须做配对差值，而不是比较各自显著性 | 一者显著、一者不显著，就证明学习有增益 |
| C2-R | R_SIM 主 T_C=-0.000334 bits/trial，CI [-0.001047,0.000301]；885 个规定读出完成 | 当前固定空间划分/读出没有推进依据；不再为同一问题扩网格 | 全部空间互补性或 PID synergy 不存在；缺失对照自动完成 |
| C2-S | 跨组均值增量约 -0.000238，中线增量约 +0.000194 bits/trial，CI 均跨零 | 几何诊断完成但没有足够预测收益，不据此开发空间新模型 | 精确重建就意味着被丢弃成分对任务有用 |
| N1 | 主 HP→HPB=-0.024957 bits/trial；背景钥匙合成世界 30/30 不可评价 | 当前拼接/MLP配置不扩种子；先修能力检验与估计器 | 背景在任何形式下均不能帮助解码 |
| N2 | k=8，57组，线性主 R(M)−R(MV) 增益=-0.001494 bits/bag；H_BAG 无EEG bAcc=0.982883、CE=0.131678 bits | 当前均值加方差表示不推进；先检查装袋后历史/时间侧信息主导程度 | 分布信息普遍不存在；高bag accuracy源于脑电 |
| N3 | 训练选族 H→HP=-0.050632，H→Hnoise=-0.051910；同logistic族 H→HP=-0.000188，CI跨零 | 主大幅负值含明显的学习能力/选族代价；改变增量估计方式 | 负互信息、EEG无信息，或噪声扣除后已经找到EEG正信息 |
| E0-R | 1024个神经头，765稳定、259未解决；规定族没有新完整MLP聚合 | 停止旧矩阵继续优化，保留数值状态 | 失败族可从成功记录中取子集作新主结果 |

研究上最值得保留的是：A2 有一个预先列出的次要线索；N1/N3 的信息增量估计尚缺关键能力验证；N2 的观察对象被元数据高度预测。不能将它们全部归为同一种“阴性”。

## 2. 对原方案的逐节修订

| 原 v2 位置 | v2.1 修改 |
|---|---|
| §4.3 数值稳定规则 | 保留旧回执；未来区分非有限失败、有限预算终点、优化诊断和任务能力，不用最后100步loss变化同时承担这些含义 |
| §4.4 与 §13.2 读出网格 | 不再普遍复制完整 MLP/线性×视图×折×正则矩阵；以完整主对比族为最小计算单元，次要族独立 |
| §5 G0 | 不从固定表征读出弱推出原始EEG无信号；把合成维数、样本数、类别率和正则尺度与实际读出对齐 |
| §6 A2 | 原主终点和Ω不变；保留R_SUP次要线索，做同人配对及缺失控制；残差反例作为解释限制而非新临床发现 |
| §7 C2 | 保留修复与几何结果；不扩大当前左右分区网格；能用旧产物完成的隔离/溯源检查单独补齐 |
| §8 N1 | 换成可精确退回基础预测的联合候选族；背景钥匙与先验反例通过后才允许新真实比较 |
| §9 N2 | 增加装袋后元数据可预测性、成员历史关系、共同支持检查；没有可比较的袋分布就不开发Deep Sets/MLP |
| §10.5 N3 | 原来仅在下一阶段考虑的offset/残差读出，现作为待验证的新估计器；不得当作旧solver修复 |
| §11 E0/B/D | B/D旧任务关闭；E1不启动；E0旧矩阵不继续补迭代，新的固定预算算法也不自动获准用于E0 |
| §12–13 能力检验与依赖 | 每条真实路线必须验证自己的正/零信息世界；缺失或数值不可评价不能当PASS |
| §15 完成状态 | 工程交付、估计器能力、观测支持、效应方向分开；不以全轮完成等同于科学完成 |

## 3. 必改一：把估计器能力检验放到真实比较之前

### 3.1 现有门控的空缺

`auditory_next/gates.py` 核验代码/测试快照、任务目录和 integration 完成；它没有将该路线合成识别能力的通过回执作为 `real=True` 的必要条件。[S5]

这不使已经计算的真实损失无效，但会使大量真实结果最终停在“有数值、无充分科学解释”。本轮 N1 的 BACKGROUND_KEY、N3 的 EEG_INCREMENT/HISTORY_ONLY 都存在这样的限制。[S3]

### 3.2 新依赖

```text
不可变历史结果
  ├─ 既有产物复核（不新拟合）：G0、A2配对与控制清单、N2元数据支持
  └─ 新估计器实现
        → 目标函数/范围/零增量退回单元测试
        → 独立合成开发集（调算法，只能用合成）
        → 冻结估计器与门槛
        → 未参与调参的合成评估集
        → capability_receipt
        → 仅通过对应能力门控的真实工作包
```

A2 的代数反例与 N1/N3 分类能力不能互相背书。通过一个强方差世界也不能替代N3保持强历史基线的检验。

### 3.3 能力回执必须记录

`estimator_id`、源码/配置hash、特征维数、每组样本数范围、组数、权重归一化、P_nat/P_bal、生成机制、理论/数值oracle、正/零信息实际分母、不可评价数、恢复率/误报倾向及其不确定性。

在看新合成结果前冻结数值容差、最低恢复要求和允许的假阳性倾向。门槛是算法筛查规则，不是临床标准。不要事后把“做完了30次”解释成预先规定了通过率。

## 4. 必改二：增量模型必须能够保留基础模型的预测能力

### 4.1 为什么N3的主负数不能直接解释为条件信息

在同一分布和log loss下，总体风险差满足：

\[
L(q_H)-L(q_{HP})
=I(S;P\mid H)
+\mathbb E KL(p(S\mid H)\|q_H)
-\mathbb E KL(p(S\mid H,P)\|q_{HP}).
\]

这是总体恒等式；有限测试估计另有抽样误差。后两项是各预测器的条件分布逼近误差。更强的条件信息可能被更差的读出器抵消，风险差为负不是负MI。

本轮 H 的五折都选 MLP，增强视图都选 logistic。完整同logistic族的 H→HP 接近零；大幅退步主要发生在神经增强族及选族主比较中。噪声扩维也造成相近退步。[S2,S6]

不能直接把 Hnoise 与 HP 风险相减叫作“修正的CMI”；两种输入造成的优化/估计误差不一定相等。

### 4.2 新估计器的最小结构

冻结基础预测器 `q0`（包含其校准），令 `ell0=logit(q0)`。使用固定特征映射 phi 学习：

\[
q_\theta(S=1\mid H,P)
=\sigma\{\ell_0(H)+r_\theta(H,P)\}.
\]

强制存在 `r=0`，逐试次精确复现 q0。先考虑低维固定映射上的线性/受限交互修正；目标可写为加权BCE加明确L2。输入维数与正则尺度需要独立验证，不强制复用MLP32。

对于N1：先拟合 `q_HP` 与 `q_HB`。联合候选族中保留它们原样的预测，以及预先指定的少量条件修正。**最终两个方向的增益必须使用同一个训练内选出的q_HPB**，不能各取一个不同的最好联合模型再合称双向信息增益。

背景钥匙的必要交互可以先由低维固定乘积映射测试，不必通过不断扩大MLP来寻找。真实映射降维参数只拟合训练数据。

### 4.3 必须守住的拟合/校准条件

- 源基础模型在内层验证中的logit必须由未使用该验证儿童的过程产生；不可使用过拟合训练logit作为“无偏offset”。
- 保留每折独立坐标，只汇总预测/标量，不跨编码器拼embedding。
- 复用旧D-inner只得到部分校准覆盖时，继续明确报告该事实。不同候选族用同一校准候选集合；不得把未被encoder排除的样本悄悄加入。
- 校准后的 q0 必须仍然是增强候选族中的精确候选；单独给增强模型缩放整个logit，可能破坏零残差退回关系，需专门测试。
- 不把从训练/验证里选择q0回退理解为测试风险必然不增。新真实负增益仍需原样报告。
- 噪声和重复输入对照使用同一基础预测、同一残差容量、同一选择预算和权重。
- `NEW_ESTIMATOR` 必须作为新版本；它不是旧独立拼接模型的等价求解。

### 4.4 新真实实验之前必须过的合成情况

1. 强且非线性的 H 基线已经能较好预测S，P独立无用：增强不能因重学H而系统性丧失已有预测能力。
2. 同样强H下，S仍有随机剩余，P提供已知的额外证据：能够恢复正增益。
3. 近乎确定的H：明确oracle剩余不确定性很小，不把无增益叫作读出失败。
4. 试次级背景钥匙与儿童级稳定背景钥匙分开；B必须提供可跨儿童使用的解码条件，而不是一个无法映射的新ID。
5. H-only、复制H、独立背景、真实加性去干扰分开。
6. 零残差逐试次预测一致性达到数值容差；不得只比较aggregate accuracy。

## 5. 必改三：N2先修观测设计，再讨论分布模型

### 5.1 本轮最重要的不是MV略负，而是H_BAG本身极强

无EEG的H_BAG已达到bAcc约0.98288，CE约0.13168 bits/bag。[S1,S2] 这说明当前同类装袋后的实验元数据高度预测标签。

它**不证明代码直接读取了当前标签，也不证明剩余EEG信息为零**。H_BAG的历史比例和时间跨度本来就可受刺激序列与装袋机制影响。0.13168是当前基线风险，不是条件熵的精确估计。

### 5.2 先做有限、不读取新EEG效应的检查

- 冻结原bag_id与成员表，追踪H_BAG每列的来源。
- 按历史比例、gap、位置、块数/跨度拆分元数据预测能力；只用于解释设计，不据此删除强预测列并宣布控制完成。
- 检查某个bag成员的事件是否作为另一成员的previous_event或更早历史出现；报告实际比例。此项是待核查关系，不能在未核验前写成已发现标签泄漏。
- 检查两类袋的历史组合是否有真正共同支持，以及同一儿童/half中是否能构造对应比较。
- 检查已知同类集合的任务定义与自然未知EEG流解码的区别；不混称部署任务。

### 5.3 有共同支持时的有限v2.1设计

先在元数据层定义有限的共同历史分层与装袋规则，再冻结。不同当前刺激采用相同的层配额、bag预算和时间约束，不通过增加有放回重采样虚增试次。

主比较仍是同一袋上的MU、MU_VAR和MU_DUP，先只用一个经能力验证的低复杂度读出。保留H_BAG和pre对照，不能为了得到EEG收益而删掉它们。

没有共同支持时标 `OBSERVATIONAL_SUPPORT_LIMITED`，N2真实分布解释停止。允许保留原任务的预测描述，但不能把它变成控制了序列结构的听觉分布证据。

新装袋任务改变了评估人群/观测机制，不是旧负结果的重新计算。必须同时保留原k=8结果及覆盖变化。

### 5.4 N2合成例也需要补真实难度

当前COVARIANCE_SIGNAL逐袋强制去均值，能够检查VAR通路，但不代表一般独立重复噪声下的检出功效。[S8]

需增加不逐袋去均值的等总体均值、不同协方差世界，保留真实样本均值的随机性。**总体均值相同，不等于有限bag样本均值不含类别信息**；例如样本均值方差可随类别变化。比较应检验VAR在MU之外的条件增益，而不是要求MU严格机会水平。

加入类不平衡序列、同类装袋、时间相关噪声和强H_BAG机制。用已冻结的真实组数/袋数/维数范围评估，不用非常强的注入结果证明微弱真实效应“理应被检出”。

## 6. A2：保留次要线索，但不改写主终点

### 6.1 当前允许的有限工作

优先复用既有49组、冻结Ω、原折与未校正差分，补：

- `T_SUP - T_SIM` 与 `T_SUP - T_RAND` 的同候选、同折配对统计。
- 既有post−pre的配对量与质量/背景对照缺项。
- fold、候选影响和表征范数敏感性，不删除不利候选，不用新筛选选出“有听觉反应的儿童”。

这些是旧结果见过之后的新审阅分析，应如此标记。不同表征的余弦T不是同一MI单位；上述比较只能说明指定统计下的匹配行为，不能直接解释为信息比特差。

如果未校正R_SUP线索在必要对照下仍保留，后续才考虑一次预先冻结的新重复；不自动为所有模型、所有旧路线开启新seed。这个复核不使R_SIM主结果变阳性，也不等于独立新队列确认。

### 6.2 背景校正反例应当如何收束

合成null中，固定非零共享W使残差100/100通过单世界重复性筛选，而未校正差分仅5/100；但同一个null机制中，训练拟合W对应6/100。[S3]

因此正确结论是“共享预测项**可以**制造残差重复性，必须检查”，不是“所有训练背景校正都必然虚假”。真实P本身可重复、R未显著，不支持将残差当刺激特异性证据。

后续不为让残差转正开发更复杂校正器；保持代数分解与反例为方法审计证据即可。

## 7. C2、G0与E0的收尾范围

### C2

保持完整R_SIM修复结果和空间重建证据，当前不再扩大左右分支矩阵。缺失的继承隔离/来源检查能用旧产物完成时单列补齐；不能为了把四轴全部变PASS而启动大量新的平行拟合。

“对当前读出没有明确增量”和“所有空间互补机制被否定”是不同结论。

### G0

保留同儿童/跨儿童的训练对象差异和共同测试集合。固定表征的同儿童读出仍弱，削弱“只缺个体校准”的简单解释，但未测试任意端到端个体解码。

先完成已有训练拟合记录、原CNN head与后拟合probe的区分；补功效时采用与真实数据接近的组数、每组样本数、64/400维输入及权重。原每组8次的夹具不能当整条encoder流程的功效证明。[S3]

本阶段不开展新的高参数encoder训练；也不把“0新encoder”写成永久禁止改进表征。算法是否扩展应由合格的信号对象和明确不足决定。

### E0/B/D

B/D旧任务继续关闭，E1继续不启动。E0不再重复原1024头优化。若未来改用固定预算有限预测器，必须另立估计器并重新冻结完整比较，不能把765个成功头事后抽出来拼成旧主结果。

## 8. 必改四：区分数值失败、预算算法与模型能力

现有`readouts.py`以有限参数、目标不劣于初始化、最后100步相对变化≤1e-4等判据决定`OPTIMIZATION_STABLE`。学习率同时按余弦趋向零。[S7]

小步长能使loss变化很小，但并不能证明已达到足够好的解；相反，仍缓慢下降的有限预测器也可能仅因该阈值被扣留。当前回执没有将这些情况全部分开。

建议未来新增而非覆盖的字段：

```text
artifact_integrity_status
finite_prediction_status
budget_execution_status
optimization_diagnostics
capability_status
support_status
control_status
scientific_effect_status
```

- 非有限loss/参数、错误权重、范围泄漏等仍是硬失败。
- 完成固定预算且预测有限可以记录为 `FINITE_BUDGET_ESTIMATOR`，但前提是这个算法定义在新评估之前冻结，并通过适配的能力检验。
- 优化梯度、目标变化、参数变化、学习率和预测稳定性分别保存。不用任何一个指标单独代表“科学任务能读出来”。
- 不根据测试效果挑checkpoint、延长某个候选或选择赢家seed。
- 旧`OPTIMIZATION_UNRESOLVED`保持原样。新算法不追溯性修补历史验收。

### 完整性规则的粒度

必须要求**一个主对比所需的全部成员**在相同候选集合上有效。但不同预先声明的模型族可以独立评价：例如N2主logistic完整、次要MLP失败，应分别报告，不把前者自动变成未评价，也不把后者缺失抹掉。

类似地，不能从失败主族中挑成功人群、成功fold或成功超参数事后汇总。新训练算法可以预先定义确定性的基础模型回退规则，完整报告每次回退；该规则属于新学习器，不是“删掉失败”。

## 9. 合成程序需要改的具体地方

| 现有构造 | 局限 | 新检验应补充 |
|---|---|---|
| N1背景钥匙按试次随机产生 | 不等于儿童级稳定背景条件 | 两种层次分开，训练与测试儿童独立 |
| N1/N3的自然权重但生成标签实际上近乎平衡 | 未覆盖真实类别率及先验失配 | 固定多个不平衡水平、同权重训练与评估 |
| N3 EEG_INCREMENT的H基本不提供目标信息 | 未检验在强H基础上保存基线并恢复小增量 | 强非线性H＋非零剩余随机性＋已知P增量 |
| N3 HISTORY_ONLY中标签几乎由H确定 | 主要是零剩余空间/上限情形 | 另设非确定H、P为H副本或独立噪声的零增量情形 |
| N2方差例对每袋去均值 | 强构造不代表一般重复观测 | 保留抽样均值随机性，并加入时间依赖和历史选袋 |
| G0每候选8次、64/400维 | 与当前实际样本结构不一致 | 基于聚合计数的维数、样本量、相关性与弱/强注入检查 |
| 原合成分类只评价raw，无完整真实校准/选族 | 不能验证最终被报告的程序 | 同一权重、transform、fit、校准、回退和聚合路径 |

所有改动在新的合成开发/评估分离中记录。固定强注入只能确认“管道能读到已知信号”，不能据此对真实阴性作无信号证明。对照不通过，暂停相应真实重跑，不继续堆结果后在最终报告列MISSING。

## 10. 实施分期与止损

### Stage 0：只复核现有产物

确认基准commit、`final_002`、原模型/预测/分割hash、运行状态和GPU政策。产生三项可交付：A2次要线索的配对计划及缺失控制表、N2历史支持/成员关系审计、N1/N3固定族与选族风险分解。

不要修改已有预测，不重新跑90任务，不恢复失败E0族。涉及受限预测的聚合仍在Slurm执行并留private。

### Stage 1：只实现和验证新估计器

先冻结小型主矩阵、合成世界、数值/恢复门槛和独立合成评估seed。优先一个可解释的受限增量读出器，不同时开发多个深度架构。

有限预算与明确终态优先于大网格。全部主成员作为一个包保留，次要族不作为主包启动前提。新代码/配置使用新目录，不修改旧auditory_next快照。

### Stage 2：有条件的有限真实实验

- A2：只对预定线索补控制，不换主终点。
- N1/N3：只运行通过对应能力门控的新主比较；共享单元必须在候选、权重、特征、折及校准范围完全一致时才复用。
- N2：只有装袋后的共同支持和元数据解释清楚，才执行一次新装袋任务的主线性比较；不直接开发集合网络。
- C2/G0：只完成有明确解释价值的既有产物收尾。

本修订不预授予大规模新编码器、所有seed、所有k和所有临床终点搜索。任何新真实结果仍是已探索队列上的新探索，不是未触碰确认样本。

### 止损条件

关键合成控制仍不可评价、基线无法逐试次复现、装袋共同支持不存在，或需要不断调整窗口/标签/人群才有结果时，停止相应路径并记录具体原因。不要以模糊的“再调调超参数”继续消耗预算。

不能仅因为一个固定架构不成功就无限推广到脑内信息不存在；也不能无限以“也许换架构”延长没有支持的研究对象。

## 11. 运行与隐私要求

沿用仓库：所有环境探查、依赖安装、测试、数值分析和模型运算经Slurm；原始EEG与临床表只读；不公开个体预测、真实身份/日期/原始路径、模型权重。新运行目录不可覆盖旧目录，结果与源码快照绑定。

后续 **禁止P100**。优先A100/L40S/H100，备选依仓库政策为V100/PRO6000；不因空闲自动回退到P100。历史P100作业保留硬件事实，不据此追溯改变结果或获得新使用许可。[S9]

先生成精确的fit预算，区分主对比、能力测试和次要研究。沿用并发上限，并将新的累计预算独立登记；不将“旧轮有剩余预算”当作无限追加许可。

## 12. 建议最终交付

以下为建议新增文件，不是当前已存在CLI或产物：

```text
V2_1_CHANGELOG.md
ESTIMATOR_CONTRACT.md
CAPABILITY_DESIGN_LOCK.json
capability_receipt.json
A2_SECONDARY_SIGNAL_REVIEW.md
N2_METADATA_SUPPORT_AUDIT.md
BASELINE_PRESERVATION_TESTS.json
paired_effects_v2_1.csv
ROUTE_DECISIONS_v2_1.md
```

每项结论必须指出：既有证据还是新估计；主还是次要；数值状态；控制状态；观测支持；固定预测CI还是全流程不确定性；具体哪些替代解释还存在。

## 13. 给执行代理的摘要

> 保留`79b3521`及其final_002为不可变历史。本次不是把v2整轮重跑。先区分优化未完成、估计器能力不足、支持不足和当前估计的阴性。A2保留R_SIM主结果，有限复核预定R_SUP次要线索并比较随机基线，不以显著性差异代替配对差值。C2不扩原左右矩阵。N1/N3先在合成数据上验证保留基础预测器的条件增量读出；能力未通过不再开对应真实任务。N2先审计同类装袋后的历史/时间可预测性与共同支持，不删除强元数据基线来追求EEG阳性。B/D/E1不重启，E0不继续原矩阵救收敛。所有新步骤另设版本与预算，禁止P100，不提交或公开受限数据。

## 来源与核查范围

所有仓库来源均固定到 `79b3521ab9b3fdd533f51192b4ba2e7937509c7a`。以上运行情况来自仓库回执和报告，不是实时Slurm连接。

- [S1] `README.md`；`reports/auditory_next_v2/final_002/NEXT_ROUND_REPORT.md`。
- [S2] `docs/AUDITORY_NEXT_RESEARCH_DECISIONS_v2.md`。
- [S3] `docs/AUDITORY_NEXT_SYNTHETIC_INTERPRETATION_v2.md`。
- [S4] `results/auditory_next_v2/A2_core_001/repeatability_aggregate.csv`。
- [S5] `auditory_next/gates.py`。
- [S6] `results/auditory_next_v2/N3_core_001/paired_effects.csv`；`auditory_next/context_execution.py`。
- [S7] `auditory_next/readouts.py`；`auditory_next/fitting.py`。
- [S8] `auditory_next/synthetic_worlds.py`；`auditory_next/n2_features.py`。
- [S9] `docs/GPU_SCHEDULING_POLICY.md`。
- [S10] 原 `AUDITORY_NEXT_ROUND_SERVER_PLAN_v2.md`，重点§4、§8–10、§12–13。
- 优化器参数语义辅助核对：PyTorch官方`torch.optim.LBFGS`文档；一阶梯度容差与函数/参数变化容差是不同终止指标，不能与本仓库固定Adam学习器的能力混同。
