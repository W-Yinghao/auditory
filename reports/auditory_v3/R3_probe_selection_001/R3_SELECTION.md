# R3 probe selection

All selection encoders and probe transforms exclude the fixed validation identities. Each probe's three penalties were evaluated only on that validation split. Outer-test EEG was not passed to selection feature inference. Incomplete grids retain missing choices.
