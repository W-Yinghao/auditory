> GitHub发布副本：本轮验收后，用户明确要求发布代码与聚合结果。文内“未发布／未push”及早期进行中表述是历史状态；当前以v2.1最终科学报告和final_001为准。个体数据、预测、模型与详细日志仍在服务器。见[发布范围](../../PUBLICATION.md)。

# 有条件真实主比较锁

在新真实结果之前固定N1/N3×R_SIM/L0四条lane。每条需要对应独立能力PASS、相同估计器/权重/维数和全部五折preflight PASS。任一必要条件未满足，生成条件停止回执、零新拟合，不改用通过的fold子集。主R_SIM、次要L0；L0来源不满足校准身份隔离时禁止旧输入自动继续。

每折使用固定D_inner0导出的原始encoder-output P/B，不能用projector或旧context变换。原始25列H顺序由预检确定；有独立missing indicator的NaN按旧固定规则置0，无全数据拟合填补。每折A-only标准化/PCA、B温度、C增量、D选择、E测试。哈希21101分割，noise为21287种子+packet命名空间+trial_id的确定性独立正态，生成不读取标签；复制输入控制沿用同一程序。

N1拟合HP/HB/HBnoise基础及HPB/HPBnoise/HPP增量，共9头/折；同一个D选择后的HPB E预测用于两个主方向。N3拟合H基础及HP/Hnoise/Hdup，共4头/折。所有候选数、λ.01、200步与正式能力评估相同。无选seed、无换窗口、无人群/临床搜索。

真实汇总合并五折独立E身份的标量损失，每身份等权、身份内试次等权P_nat。固定预测身份bootstrap2000次、seed222999，两个方向和控制使用相同身份draw。区间不是整个学习流程重拟合区间。N1同时报告HP→HPB、HB→HPB及相对噪声/复制的优势；N3报告H→HP、H→Hnoise、H→Hdup及两个控制差值。科学推进要求主方向及必要控制差值区间下限均>0；否则保留阴性/控制不足，不能声称无脑电信息或负MI。

完整主包最多N1两lane90头、N3两lane40头，共130头、40温度、140变换；能力未过的lane计0实际拟合并保留计划分母。保存每次调用、优化诊断、全部预测及hash；个体数据/模型仅private。新结果属于已探索队列上的新算法探索，既不回填旧v2，也不是新独立临床验证。
