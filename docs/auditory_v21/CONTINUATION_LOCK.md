> GitHub发布副本：本轮验收后，用户明确要求发布代码与聚合结果。文内“未发布／未push”及早期进行中表述是历史状态；当前以v2.1最终科学报告和final_001为准。个体数据、预测、模型与详细日志仍在服务器。见[发布范围](../../PUBLICATION.md)。

# v2.1 剩余工作执行范围

用户已要求继续提交并完成全部v2.1计划。现有首批结果和失败回执保留。后续各路线必须形成完成或有证据的条件停止结论，不能将“作业算完”当作科学通过，也不能把可完成的预检留成待办。

首先执行零拟合输入预检、A2/G0/C2源检查；N2在查看新EEG效应之前冻结共同历史配额的无放回bag构造和原袋元数据分组诊断。N2的元数据诊断限定5个视图×5折=25个低复杂度头，lambda0.01、200步，原H_BAG完整视图保留。精确构造细则另见N2_DESIGN_LOCK.md，不按EEG效果选层。

N1/N3采用首批已描述的单一估计器，不开参数网格；25维H、R_SIM的64/64及L0的400/200维P/B，使用预检核实的每折角色组数/试次数。正式独立能力评估锁在新的配置中。两个开发种子21101/21102绝不进入正式评估。强世界门控与弱注入描述分开，零世界必须检查风险损失、未阈值误报和有最小效应门槛的误报。来源/能力缺失不会触发真实对比。

仅通过对应能力且五折来源/类别支持完整的路线，执行一次冻结的P_nat真实主包，R_SIM为主、L0为预定次要；N1两个方向共用一个联合模型。L0若旧scaler与校准角色交叠，旧L0输入阻断，不能用“无神经编码器”豁免范围要求。全套控制和失败分母保留，不从成功fold挑子集汇总。

预检源码核对补充：实际L0导出位于probe/scaler拟合之前，为固定20ms分箱；只有核实冻结源码与feature hashes证明该无拟合路径，才使用空encoder/scaler范围的stateless回执。旧probe训练组单列，不误认为L0原始特征训练组。仍保留离线QC等继承限制。

N2只有新设计支持存在才执行匹配袋规模的独立分布能力检验，再决定MU/MU_VAR/MU_DUP、H_BAG及pre对照的单一低复杂度真实对比。不创建新encoder，不重启E0/B/D/E1，不扩C2网格。A2当前配对比较无清晰学习优势，因此不自动开启新seed重复。

全部数值、测试、哈希、绘图和拟合通过Slurm；GPU禁止P100。本轮192 CPU core·h/16 GPU·h硬上限与4CPU/2GPU并发不变，首批已按12.5 CPU core·h保守记账。后续先记精确fit与申请预算再提交。完成后提供源码/配置/输入hash、能力回执、真实或条件停止结果、路线决定及完整复现入口。GitHub发布需单独的明确指令。
