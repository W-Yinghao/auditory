> GitHub发布副本：本轮验收后，用户明确要求发布代码与聚合结果。文内“未发布／未push”及早期进行中表述是历史状态；当前以v2.1最终科学报告和final_001为准。个体数据、预测、模型与详细日志仍在服务器。见[发布范围](../../PUBLICATION.md)。

# v2.1 启动与范围锁定

用户已明确要求启动 v2.1。基准为79b3521及final_002，原始数据、旧源码、配置、预测、模型、失败回执不修改。新目录统一使用auditory_v21，数据依赖以哈希绑定。新的阴性或阳性都不回填v2。

Stage0配置configs/auditory_v21_stage0.json在新配对统计之前冻结：A2只复核49组原Ω中的SUP−SIM、SUP−RAND（post与post−pre），保留主R_SIM；N2只查保存袋成员与完整事件历史，不拟合分类器、不打开EEG特征；N1/N3分列原固定族及训练选族风险，G0/C2只补已有证据解释。候选级影响只报告范围，不删人。

Stage1先实现一个低复杂度、可精确退回基础预测的增量学习器，并用独立的合成开发与评估验证。新真实比较必须等待对应能力回执PASS；开发结果不能当成评估通过。完整性按预先声明的主对比成员判断，次要族独立报告。学习器定义、校准方式、映射、数值容差、能力门槛、独立seeds及精确拟合目录在运行相应实验前另行冻结，不照搬旧拼接网格。

独立本轮硬上限192 CPU core·h、16 GPU·h，最多同时4个CPU作业与2个GPU作业；Stage0预留8 CPU core·h、0 GPU·h、0头/编码器拟合。按每个已提交作业的申请上界保守累计，达到上限停止。算力是上限而非用满目标；不继承旧轮“剩余预算”。Stage1/2拟合次数必须按新目录逐项计数后才能提交。

全部数值、测试、探针、哈希和绘图通过Slurm。提交前umask077，private目录0700、文件0600。GPU默认A100，禁止P100，备选遵循GPU_SCHEDULING_POLICY.md。当前没有新编码器、E0重拟合、B/D/E1重启或自动Git发布。

本文件为启动记录，尚不代表任何新实验已完成或通过能力门槛。

实施补充：首个模块测试tests_001使用了Stage0的配置入口，但其中包含Stage1合成估计器测试（计划代码可推导39次头调用），不能记为Stage0零拟合；测试失败原因保留在原回执。之后测试单独使用development配置及逐调用硬计数，Stage0正式审计仍严格0拟合。这是测试编排修正，不修改原测试快照或科研结果。

首批执行补充：tests_004通过26项测试；stage0_001/job997435与development_001/job997436已完成，launch_review_001/job997439复现旧A2点估计并完成新聚合。36/36合成开发世界、244/244头全部完成。尚未执行独立能力评估，真实门控仍关闭；具体效应及剩余工作见ROUTE_DECISIONS_v2_1.md与LAUNCH_RESULTS.md。各作业启动时快照维持原样，不随本启动记录的后续增补改变。

最终执行补充（取代上一段的当时状态）：880/880独立能力世界及四个条件真实lane全部完成；N2设计支持存在但4/5外折角色支持不足，按冻结规则停止对应分布分支。tests_009通过59项，最终审计closure_003，图figures_002，final_001/job997550返回FINAL_AGGREGATE_COMPLETE。具体科学结果及限制见SCIENTIFIC_RESULTS_v2_1.md；保留所有中间失败/修订回执，不将新估计器回填旧v2成功。32 CPU core·h申请上界、0 GPU、新正式encoder为0，无自动GitHub发布。
