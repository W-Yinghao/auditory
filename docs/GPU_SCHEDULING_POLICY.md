> 发布副本：用户在本轮验收后明确要求将代码与聚合结果推送到GitHub。文内“未push／不自动发布”描述该授权之前的状态；个体数据、模型及详细日志仍不发布。历史进行中状态以final_002为准。见[发布范围](../PUBLICATION.md)。

# GPU 调度偏好

用户明确要求：后续不得使用 P100；优先 A100、L40S、H100，最低选择 V100 或 RTX6000PRO（PRO6000）。不能因 P100 空闲而自动回退。

`auditory_next_gpu.sbatch`、兼容性探针入口和当前站点默认分区已改为 A100；两个 GPU 脚本也拒绝通过命令行覆盖为 P100。若首选分区排队，再检查 L40S、H100、RTX6000PRO、V100 的实际资源；仍遵守既定并发与算力总预算。全部数值任务继续通过 Slurm。兼容性探针是已完成的一次性历史运行，不重复提交；将来的探针须另建运行名。

本次变更时 E0-R 尚未启动，已转至 A100。C2-R 与 synthetic 已在 P100 运行，保留本次进度与硬预算，不中断重拟合；其硬件记录保留为历史事实，不构成后续使用许可。旧源码快照和已完成结果不改。
