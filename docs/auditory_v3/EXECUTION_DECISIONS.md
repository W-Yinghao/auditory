> GitHub 发布副本：本轮执行完成后，用户明确要求发布代码与聚合结果。文内“未 push”和早期运行状态是历史记录；V3 以 final_002 为准。个体数据、预测、模型和详细日志留在服务器。见[发布范围](../../PUBLICATION.md)。

# v3 implementation and execution decisions

The supplied design and YAML are immutable. The execution copy is configs/auditory_v3_plan.yaml. This new round is authorized by the researcher's explicit instruction to read and execute the two supplied documents. It does not reopen old experiments or automatically authorize a new publication.

S0 resolves inputs from completed v2.1 n2_design_001 and the exact legacy plan_001, splits_001, S1_support_004 and P1_CAUSAL20 exports, rather than searching by newest directory. The initial registry was Slurm997615; support_002/997617 is the successful support freeze. support_001 is a preserved adapter failure comparing numeric half values as strings, corrected without modifying input values. P_MATCH contains49 distinct candidate identities mapped to49safe split groups,698bags,5584unique member trials. All98group×half×history/record cells support R3 quartets with minimum10.823s same-class separation and nonoverlapping direct epochs.

Outer mapping is unchanged. N2R inner GroupKFold uses one sorted identity per row. R3 holdout sorts hexadecimal SHA256 of UTF8 seed|outer_fold|identity_id, seed63011, and selects max(5,ceil(.2*n_train)) validation groups. The repeated R3 minimum-support sentence in the Markdown is a duplicate, not a second split requirement. R3's actual readout matrix is240heads (180selection and60final), below its280 ceiling. No calibration or extra architecture is introduced.

P0's finite target check uses15worlds/30heads plus2separately counted equivalence fits. Packet-specific source hashes and matching test/capability receipts are checked before real feature loading/fitting. Test/mock optimizer calls are distinguished from actual fits; all real optimizer attempts, including numerical continuation, register atomically against the3000round ceiling before execution.

Jobs execute private source snapshots. Capability and real jobs are submitted with AUDITORY_V3_CODE pointing to the passing test run's immutable source directory, so queue-time edits cannot change their algorithm. New versions require their own tests/receipts. Arrays for learned encoders remain private. Full historical arrays and model outputs are not exposed in public summaries. Training targets, gates and outcome labels remain separate from implementation completion.

Capability mechanism definitions and any allowed one-off development revision must be frozen before their respective independent evaluation seeds are used. No generator is adjusted from real outcomes. R3 data support and training smoke are independent of P0/N2R real significance. Future final reporting must keep all three packet axes, pre-window limitations, the explored-cohort scope and fixed-OOF bootstrap limits.

N2R capability preserves the exact frozen698bags/5584member rows and metadata, including identity,half,historycell,block,timing and outer/innerfolds. Only synthetic numeric responses are generated:8latentGaussiancoordinates redundantly mixed to400/200 dimensions with small observationnoise, identity backgrounds and time-correlated shared bagmeans. VAR_EXTRA residual SDs0.35versus2.0 are not demeaned. MEAN_SUFFICIENT uses a fixed mean shift and common within-bag Gaussian residual covariance; HISTORY_ONLY makes the template class a deterministic function of an explicitly supplied synthetic H gap field and response depends on that H. These nulls have no injected variance increment. Independentdevelopmentseeds50001–50003 passed; no revision or realEEGeffect tuning. Evaluation53001–53060 uses the identical nested HQ/HQV core and the exact frozen metadata distribution. All numeric fits, model artifacts and identity predictions remain private.

R3 numerical amendments are explicit, versioned and not effect-driven model changes. Native CUDA adaptive pooling rejected strict deterministic backward before the first optimizer update. A parameter-free equivalent floor/ceiling-bin mean implementation passed33tests; all original states/seeds/exposures were retained in3same-initial-state continuations. The first attempt to cancel MATCH raced with its start; the ledger and subsequent reports correct that provisional scheduler observation. All3initialfailures and1budget-blockednewallocation remain accounted for.

R3 selection then exposed probability rounding: finite logits can produce floating-point probabilities exactly1, so probability-domain log loss became infinite in4complete lambda grids. The180coefficient fits themselves were retained. A separately tested logit scorer evaluates the same uncalibrated logistic cross-entropy with logaddexp and uses raw logits for monotone AUC ranking; it does not clip predictions, alter regularization, discard a lambda or refit a model. The original source and failed-score receipt stay immutable. All180selection scores are recomputed from saved models on inner validation only before finaltraining; allfinalrisks use the same corrected scoring. New scoring hash/test provenance is recorded in addition to the unchanged basefit algorithm hashes. This remains an explored-cohort analysis with a disclosed numerical scoring correction, not a new untouched preregistration.
