# Auditory：FN1-A 更新审阅与下一步路线

**日期：** 2026-09-19  
**核查版本：** `5b5b662c4d7a2f7dc9dc5499ad0ad6f0add012c0`  
**性质：** 代码与已公布结果的审阅、下一轮建议范围；没有运行服务器上的原始数据，没有修改 GitHub 或提交 Slurm 作业。  
**约束：** 只使用已有资料；不联系医生，不要求新问卷、同期确认或新增采集。未知版本、日期关系、设备状态继续作为解释限制，而不是新等待条件。

## 0. 决策摘要

不扩展现有“先学习后汇总”方法主线，也不将当前较大负增益解释为 EEG 没有信息。

下一步分成两层：

1. **K0/K1：完成一次有明确原因的目标函数校正。** 这是结果收尾，不是新的论文 idea。修正的依据是代码中可直接验证的 SSE/MSE 正则化尺度不一致，而不是希望得到阳性。
2. **DYN：保留一个与均值/分布汇总不同的研究候选。** 问题是相同边际特征分布下，真实时间顺序是否提供可泛化的动态信息。当前只给出设计骨架，不自动启动新临床模型矩阵。

旧 M0/M1/M2、旧 M3/M4、失败日志、来源修正和阴性结果全部保留。没有小正值就扩模型、大负值就换终点的规则。

---

## 1. 这轮已经真正完成什么

FN1-A 从 56 条 HA 索引中得到 52 条具有足够连续信号支持的记录。每条使用真实 BDF，经既定因果 0.5–30 Hz 流程至 250 Hz，选取 32 个不重叠的 4 s 窗口，每窗 140 个固定特征。4 条因有效窗口不足而未进入模型，没有因元数据未知而排除。来源范围与限制见 [R1–R4]。

两个目标均完成 M0–M4、振幅和训练对应错配对照。分析对象是原表数字：

- `A_archive`：HA 合并 IT-MAIS/MAIS 列的原始数值。
- `V_archive_given_A`：已观察同一行 A 时预测该行 MUSS。

它们不是经过确认的同期临床能力或随访终点。当前 52 组不应仅因人数相同，就被当作上一轮重训的同一 52 组。

### 1.1 主要结果

| 模型 | A 的 MAE | 已知 A 后预测 MUSS 的 MAE |
|---|---:|---:|
| M0：临床 | 6.904 | 5.004 |
| M1：临床＋技术摘要 | 6.655 | 5.638 |
| M2：M1＋片段均值岭回归 | 6.655 | 5.638 |
| M3：先汇总后学习 | 10.298 | 8.566 |
| M4：先学习后汇总 | 10.243 | 8.571 |

M3→M4 为 +0.055 [−0.079, +0.185] 和 −0.005 [−0.101, +0.097]。这支持“固定配方下未建立学习顺序优势”，不支持“所有学习顺序在总体上等价”。

M2 在两个目标的全部外折选择 EEG-off，所以其预测与 M1 相同。零宽差值区间是选择结果，不是精确零信息区间。[R1,R2]

---

## 2. 新发现：临床惩罚不是同一个尺度

### 2.1 代码证据

`auditory_fn1/models.py::ridge_solution` 实际求解：

```python
gram = centered.T @ centered + np.diag(penalties)
coef = np.linalg.solve(gram, centered.T @ (y - intercept))
```

因此它对应：

\[
\sum_{i=1}^{n}(y_i-b-C_i\gamma)^2+\alpha\|\gamma\|^2.
\]

`train_set_network` 使用记录平均 MSE：

\[
\frac1n\sum_{i=1}^{n}(y_i'-b-C_i\gamma-f_\theta(U_i))^2
+\alpha\|\gamma\|^2
+\frac{\lambda}{2}\sum_{W\in\theta}\|W\|_F^2.
\]

`auditory_fn1a/fitting.py::_train_network` 将 M1 选出的 `clinical_spec["penalty"]` 直接传给上述平均损失。两者仅共享数值，没有共享有效正则化强度。[R5,R6]

### 2.2 正确的尺度关系

将岭回归目标除以 n，等价形式应为：

\[
\operatorname{MSE}+\frac{\alpha}{n}\|\gamma\|^2.
\]

因此目前神经模型临床块的有效惩罚，是相应岭回归临床块的 n 倍。n 是**该次实际拟合的记录/身份数**，不是 52×32 个窗口，也不是全队列人数。每个内折和最终外折拟合都要分别计算。

目标从 0–100 缩放至 0–1不会消除这个因子：系数与截距同步缩放后，仍须从 SSE 到 MSE 除以 n。

标准 Ridge 的 SSE＋alpha 定义也见官方文档 [R9]。这是代码与代数结论，不是通过真实临床结果选择的解释。

### 2.3 能说什么，不能说什么

- 能确定：M1 与 M3/M4 的临床块未受到匹配的惩罚；原“只差 EEG”的解释不成立。
- 不能确定：全部约 3 分 MAE 退化都由此引起；400 步优化、有限样本和特征质量仍可能有影响。
- M3 与 M4 受到同一约束，所以其固定算法比较仍可报告；但不能扩展为一般性无效。
- 这是原方案没有统一损失尺度留下的规格问题，不能只归责为服务器没有跑够 epoch。

---

## 3. 其他解释修订

### 3.1 错配对照不是条件信息证明

A 上真实对应相对训练错配改善约 +3.697 分，但真实模型仍明显差于 M0/M1。[R1]

错配同时破坏 EEG 与目标、临床变量、技术摘要的关系。因此，只能说训练对应影响了该学习流程；不能推出独立于临床变量的 EEG 功能信息已经成立。

简单反例：Y=C，Z=C+噪声。在已知 C 时，Z 对 Y 没有新增信息；但临床分支过度正则化时，真实 Z 可以替它补回 C，而错配 Z 不可以。于是“真实优于错配”与“条件增量为零”可以同时出现。

### 3.2 合成检查未单独验证 EEG 增量通路

当前三个合成机制都有临床信号，但通过条件主要是优于训练均值。代码已明确披露：临床分支单独变好就可能通过。[R7]

新检查不需要更多世界，而需要正确参照：已知存在 EEG 增量时，比较临床模型与联合模型；已知只有分布信息时，再比较 M3 与 M4。

### 3.3 不回填缺失的历史优化日志

`STAGE_STATUS.json` 指出逐次训练的最终损失、梯度、训练误差等未持久化。[R2]

因此不能从测试预测收缩直接诊断“400 步必定未收敛”。旧缺失保留；新校正拟合保存这些量。不为补旧日志重复整轮训练。

---

## 4. K0：先做零 EEG 的目标函数一致性核查

**目的：** 将确定的尺度问题与未知的临床信号问题分开。

### 4.1 固定不变

复用 FN1-A 的临床行、修正 PTA、52 组支持、五外折及三内折、M1 已选临床基与 alpha。只读既有特征和配置。不要重新选临床日期、量表、身份、窗口或 QC。

不需要重新读取 BDF，也不需要重做资料资格审计。

### 4.2 数学测试

使用独立合成设计矩阵，不含个人数据，核对：

1. `SSE + alpha ||gamma||²` 的闭式解。
2. `MSE + (alpha/n) ||gamma||²` 的闭式解。
3. `MSE + alpha ||gamma||²` 的闭式解。

1 与 2 应在 FP64 精度下等价；3 应等价于 SSE 形式的 `n*alpha`，不是 1。覆盖原惩罚列表和实际内/外折训练规模。

加入目标单位缩放、常数输入列、缺失指示列、非零目标均值测试。常数列由已定义的训练内标准化处理，不改变外层数据。

### 4.3 可选的低成本量化

在原保存的各折 M1 规格下，只用临床＋技术输入，对比 alpha 与 n*alpha 的闭式解，报告固定 OOF 误差变化。两目标合计每目标五外折；这不是新调参搜索。

它用于说明临床收缩本身能造成多大变化，不把差值当成对 M3/M4 全部退化的因果归因。

### 4.4 K0 交付

- `OBJECTIVE_SCALE_AMENDMENT.md`
- `objective_parity.json`
- 每个拟合范围的 n、alpha、alpha/n、目标单位和基函数类型（身份键留 private）
- 原模型与修正目标的关系说明

K0 完成不意味着修正后的 EEG 模型会有效。

---

## 5. K1：一次解析临床块的校正性比较

**性质：** 新的校正性估计流程；不是旧结果的零拟合重评分，也不是新论文贡献。

仅修正 `alpha/n` 而继续把整个临床块从零交给 400 步 Adam，仍可能留下临床拟合预算的混杂。建议对临床线性块使用条件闭式解，而不再通过延长整个网络训练来追求基线恢复。

### 5.1 明确目标

在归一化目标 y' 上：

\[
L(\theta,\gamma,b)=\frac1n\|y'-b\mathbf1-C\gamma-f_\theta(U)\|_2^2
+\frac{\alpha}{n}\|\gamma\|_2^2
+\frac\lambda2\sum_{W\in\theta}\|W\|_F^2.
\]

C 包含对应 M1 已选定、在当前训练范围拟合的临床＋技术基。

对固定 theta，令 r=y'−f_theta(U)，对 C 在该训练范围中心化为 C_c：

\[
\gamma^*(\theta)=(C_c^TC_c+\alpha I)^{-1}C_c^T(r-\bar r),\qquad b^*(\theta)=\bar r.
\]

测试时使用训练中心。若实现未中心化 C，截距需要相应减去训练 C 均值与 gamma 的乘积，不能混用两套公式。

### 5.2 训练原则

- EEG 通路保持原 140→8→8、tanh、两种池化顺序，不新增注意力或时序网络。
- 初始化 EEG 输出为零。
- 每次 theta 更新前先精确求 gamma,b；更新 theta 后重新求解并记录当前完整目标。
- 临床块精确最优时可用包络定理对 profiled objective 求 theta 梯度；实现必须用有限差分核对，不直接假定梯度正确。
- 保留原 400 次 theta 更新、原两种子、原 EEG 惩罚候选，M3/M4 采用同一规则；不为了更好测试分数追加步数。
- 目标缩放、裁剪、拟合人数、每项正则实际值必须写入回执。

这保证每个给定 EEG 通路下临床块解的是一致目标，不保证神经全局最优，也不保证外层测试不退化。

### 5.3 必须通过的窄测试

1. EEG 输出冻结为零时，每个训练范围的预测精确复现归一化后的 M1。
2. EEG 输出是常数时，只能按截距规则改变残差中心，不制造新的临床方向。
3. 目标函数与梯度的尺度一致性。
4. 临床充分、EEG 独立的世界：报告相对临床基线的误差，而非仅对均值。
5. 明确的均值增量和分布增量世界：必须分别展示 EEG 增量及学习顺序的可读行为。

沿用少量开发构造，另用未用于修改实现的固定种子复核。不要再建立数百世界的统计门控；这些是通路能力检查，不是总体假阳性率或临床功效证明。

### 5.4 真实比较范围

复用同一 52 组、同一窗口特征、同一目标、原外内折。

- M0/M1/M2 来源完全相同时直接复用，不重新求解只为了填台账。
- M3*、M4* 的 EEG 惩罚按原两候选在内层选择；种子11/23预测平均后选择。
- 两个目标均完成，不因主目标结果决定是否运行次要目标。
- 原振幅与记录包错配对照对新 M4* 同样执行；沿用预先定义的选择复用规则，不增加新网格。
- 原 M3/M4 与新 M3*/M4* 分开报告。优化和归一化同时改变，不声称新旧差值完全来自某一项。

照原矩阵，主学习模型拟合为 2目标×5外折×2模型×(2惩罚×3内折×2种子+2最终种子)=280 次；振幅与错配最终拟合各20次，总计320次小网络拟合。不是320名儿童，也不是320个独立统计检验。

这是一轮有限 CPU 任务，不新增原始信号编码器、临床目标、窗口或数据集。计算前登记预算和拟合目录，完成后不为小正值自动扩展。

### 5.5 输出和结论

同时报告：

- M0、M1、M2、旧M3/M4、新M3*/M4*的原单位 MAE、RMSE；
- M0→M4*、M1→M4*、M3*→M4*；
- 振幅及错配对照的绝对误差与差值；
- 训练范围内 loss、各惩罚项、梯度、预测范围、临床系数及 EEG 输出量级（逐人数据不公开）；
- 固定 OOF 身份重采样区间，明确未覆盖整个学习过程的不确定性。

不新增“增益必须大于某值才能把剩余对照跑完”的执行规则。所有规定对照完成后，作研究资源决定。

| 新结果形态 | 研究决定 |
|---|---|
| 只恢复到 M1，仍没有 EEG 增量 | 归档当前临床聚合主线；不再次换聚合器 |
| M3*/M4*均有增量，但二者无区别 | 研究问题是记录信息，不是学习顺序；不宣传 M4 方法优势 |
| M4*同时超过基线、M3*及必要对照 | 保留有限档案证据；后续要单独评估重训稳定性与多次探索，不能称独立临床验证 |
| 新模型仍退化 | 记录该有限算法结果；停止本轮，不继续无上限优化 |
| 数值/来源失败 | 单独报告，不改为“EEG无信息” |

---

## 6. 真正不同的备选研究问题：动态信息，而不是另一种均值汇总

**DYN 候选问题：** 当两次记录具有相近的局部统计分布时，真实时间顺序是否提供静态集合表征没有保留的、可在独立时间段读取的 EEG 信息？

这是待检验问题，不是本轮发现的机制，也不是已经建立新颖性的 JBHI 主张。

### 6.1 为什么与 FN1-A 不同

M3 和 M4 对32窗的顺序任意置换均不变：

\[
f(u_1,\ldots,u_{32})=f(u_{\pi(1)},\ldots,u_{\pi(32)}).
\]

M4检验“每窗先做非线性再平均”，不是时间序列模型。每窗特征也是频带功率及Hjorth，不能据其阴性宣布跨窗时间信息不存在。[R3,R5,R10]

### 6.2 首轮只设计一个有限现象检验

先比较同一个低维特征序列的：

- 静态背景/边际分布预测；
- 含时间历史的简单预测；
- 保留同一组窗口但改变顺序的对照。

目标先是独立后段 EEG 特征，而非另换一个临床量表。可用留出预测损失定义“在已有背景之上的时间预测增益”，不直接将其命名为精确 CMI。

首先与简单线性自回归比较。若更复杂模型只是重复普通自相关或慢漂移，不构成值得发展的新故事。

### 6.3 必须先解决的观测定义

现有32个窗口是从合格窗口中均匀抽取，并非必然时间相邻，不能当作32个等间距观测。

需要先只读已有时间戳/有效窗清单，明确真实间隔。若复用现有数据，只能用真实间隔匹配的有限比较；若需要完整连续合格序列，应另行冻结一个只用于DYN的导出，不能偷偷复用稀疏窗口充当连续序列。

共享滤波记忆、慢漂移、眼动/运动及记录选择都可造成时间预测性。保留真实间隔和既有保护区；对照包括局部块置换及与线性时间结构匹配的替代数据。两种对照破坏的东西不同，不声称某个置换自动隔离神经机制。

### 6.4 后续才问档案价值

只有先形成定义清楚的动态对象，才考虑用同一既定 A_archive 检验它是否超出静态特征与技术摘要。仍属现有档案关联，不要求医生确认。

该后续不是本次自动启动范围。没有稳定动态现象，或只有一般自相关而无额外目标价值时，停止这个候选，不升级为大模型预训练。

---

## 7. 明确关闭与不重开

- 不再重复临床资料资格审计，不生成医生联络材料。
- 不重开旧刺激头/null分解、N1/N3条件读出、R3对比目标或旧空间网格。
- 不因A满分较多就删满分样本，不换成新的二分类阈值来追求准确率。
- 不合并HA与MFF的未知量表单位；不称混合来源为CI验证。
- 不将重复EEG日期当作真实功能随访。
- 不修补历史缺失日志而重新运行已完成模型。
- 不把K1校正本身写成信息论方法贡献。

---

## 8. 服务器交接摘要

> 在 `5b5b662c4d7a2f7dc9dc5499ad0ad6f0add012c0` 的已完成 FN1-A 产物基础上工作。先做K0目标函数一致性核查：区分SSE和MSE，验证临床惩罚应为alpha/n，n为每次实际训练记录数。不要联系医生、重新抽取EEG或改变目标与队列。
>
> K1是建议的单次校正性实验：保持原网络和数据，解析求解每个theta下的临床线性块，消除临床分支优化预算与尺度不一致；通过窄数值/通路测试后，按固定矩阵完成两个目标和原对照。保留全部原结果，新结果单独命名。不要新增主干、正则网格、量表、QC阈值或显著性执行门槛。
>
> DYN仅为独立候选问题。先做时间观测定义，不自动提交新的动态模型或临床预测队列。任何来源不足按真实不足记录，不能借此重新引入等待医生确认的锁。
>
> 所有实际数值任务遵守仓库Slurm规则。原始数据只读；模型、逐人预测、身份、确切日期与日志均留private。聚合与校正说明可本地生成，但本交接不授权自动push。

---

## 9. 证据入口

所有仓库链接固定至本次核查commit，不依赖移动的main。

- [R1：FN1-A结果报告](https://github.com/W-Yinghao/auditory/blob/5b5b662c4d7a2f7dc9dc5499ad0ad6f0add012c0/docs/auditory_fn1a/ARCHIVAL_RECORD_LEARNING_REPORT.md)
- [R2：阶段状态及缺失诊断说明](https://github.com/W-Yinghao/auditory/blob/5b5b662c4d7a2f7dc9dc5499ad0ad6f0add012c0/docs/auditory_fn1a/STAGE_STATUS.json)
- [R3：连续信号导出](https://github.com/W-Yinghao/auditory/blob/5b5b662c4d7a2f7dc9dc5499ad0ad6f0add012c0/auditory_fn1a/signal_export.py)
- [R4：执行配置](https://github.com/W-Yinghao/auditory/blob/5b5b662c4d7a2f7dc9dc5499ad0ad6f0add012c0/configs/auditory_fn1a.yaml)
- [R5：网络、岭回归和损失](https://github.com/W-Yinghao/auditory/blob/5b5b662c4d7a2f7dc9dc5499ad0ad6f0add012c0/auditory_fn1/models.py)
- [R6：实际拟合及对照调用](https://github.com/W-Yinghao/auditory/blob/5b5b662c4d7a2f7dc9dc5499ad0ad6f0add012c0/auditory_fn1a/fitting.py)
- [R7：合成检查及其能力解释限制](https://github.com/W-Yinghao/auditory/blob/5b5b662c4d7a2f7dc9dc5499ad0ad6f0add012c0/auditory_fn1a/synthetic.py)
- [R8：原FN1-A方案](https://github.com/W-Yinghao/auditory/blob/5b5b662c4d7a2f7dc9dc5499ad0ad6f0add012c0/AUDITORY_FN1_ARCHIVAL_PLAN_v1_1_493a076.md)
- [R9：scikit-learn Ridge目标定义](https://scikit-learn.org/1.8/modules/generated/sklearn.linear_model.Ridge.html)
- [R10：Deep Sets，集合输入与置换不变性](https://proceedings.neurips.cc/paper/2017/hash/f22e4747da1aa27e363d86d40ff442fe-Abstract.html)

本次审阅另外做了独立的40行合成设计矩阵代数检查：alpha=0.1时，SSE闭式解与MSE中alpha/n的闭式解最大系数差约6.94e-18；直接复用alpha时不等价。这只是公式检查，不是服务器EEG实验，也不定量说明真实性能损失的来源。
